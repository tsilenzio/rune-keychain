# Copy to .env and update with your signing identity:
#   cp .env.example .env
#
# Find your identity with:
#   security find-identity -v -p codesigning
#
# To create a self-signed cert for development:
#   https://github.com/tsilenzio/dotfiles/blob/main/platforms/macos/bin/codesign-cert
#   Or manually: Keychain Access > Certificate Assistant > Create a Certificate
#   (set type to "Code Signing", validity to 10+ years)

SIGNING_IDENTITY=your-signing-identity-here
